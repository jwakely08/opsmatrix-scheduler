node_modules/
dist/
.env
.env.*
!.env.example
*.local
.DS_Store
Thumbs.db
/Test_project_Statistics.csv
scripts/out/

# vendored at build time by scripts/copy-pdfjs.cjs
public/pdfjs/
# vendored at build time by scripts/copy-xlsx.cjs
public/vendor/

# real hospital plans used as detection benchmarks — never commit (public repo)
bench/fixtures/
bench/out/
# generated benchmark data derived from the real plan (coordinates of a real
# hospital's rooms — keep out of the public repo like the PDF itself)
bench/readings/
bench/answer-key-ew.json
bench/extra-readings.json
